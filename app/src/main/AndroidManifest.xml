<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobsoft.win.the.afor.bullshit.aut.bme.autonyilvantarto">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".AutonyilvantartoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="DATABASE"
            android:value="mobsoftlab.db" />
        <meta-data
            android:name="VERSION"
            android:value="2" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="mobsoft.win.the.afor.bullshit.aut.bme.autonyilvantarto.model" />

        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="Autók"
            android:name=".ui.carlist.CarListActivity" />

        <activity
            android:label="Autó adatai"
            android:name=".ui.cardetails.CarDetailsActivity" />

        <activity
            android:label="Autó mentése"
            android:name=".ui.newcar.NewCarActivity" />

    </application>

</manifest>